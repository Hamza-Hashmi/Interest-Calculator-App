<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:gravity="center"
        android:id="@+id/tv"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:textColor="@color/black"
        android:text="Compound interest period (in months)"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="12"
        android:background="@drawable/edt_compound_month_bg"
        app:layout_constraintTop_toBottomOf="@id/tv"
        android:layout_marginTop="10dp"
        android:padding="10dp"
        android:textColor="@color/black"
        android:id="@+id/edt_months"
        android:textSize="18sp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ok"
        android:textColor="@color/color_primary"
        android:textSize="18sp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/edt_months"
        app:layout_constraintRight_toRightOf="@id/edt_months"
        android:id="@+id/btnOk"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancel"
        android:textColor="@color/color_primary"
        android:textSize="18sp"
        android:layout_marginTop="10dp"
        app:layout_constraintRight_toLeftOf="@id/btnOk"
        android:layout_marginRight="20dp"
        app:layout_constraintTop_toBottomOf="@id/edt_months"
        android:id="@+id/btnCancel"/>

</androidx.constraintlayout.widget.ConstraintLayout>